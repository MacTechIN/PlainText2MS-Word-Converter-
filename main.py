import textwrap
from docx import Document
from docx.shared import Pt, RGBColor
from docx.oxml.ns import qn
from docx.enum.style import WD_STYLE_TYPE
from docx.enum.text import WD_ALIGN_PARAGRAPH

def create_full_stack_prompt_guide():
    # 1. 문서 생성
    doc = Document()

    # 2. 스타일 설정 (한글 폰트 적용)
    # 기본 스타일 (Normal) - 맑은 고딕
    style = doc.styles['Normal']
    style.font.name = 'Malgun Gothic'
    style.element.rPr.rFonts.set(qn('w:eastAsia'), 'Malgun Gothic')
    style.font.size = Pt(10)

    # 제목 1 스타일 (Phase)
    h1_style = doc.styles['Heading 1']
    h1_style.font.name = 'Malgun Gothic'
    h1_style.element.rPr.rFonts.set(qn('w:eastAsia'), 'Malgun Gothic')
    h1_style.font.size = Pt(16)
    h1_style.font.bold = True
    h1_style.font.color.rgb = RGBColor(0, 0, 0)  # 검정색

    # 제목 2 스타일 (1.1, 1.2 등)
    h2_style = doc.styles['Heading 2']
    h2_style.font.name = 'Malgun Gothic'
    h2_style.element.rPr.rFonts.set(qn('w:eastAsia'), 'Malgun Gothic')
    h2_style.font.size = Pt(13)
    h2_style.font.bold = True
    h2_style.font.color.rgb = RGBColor(0, 0, 0)

    # 코드/박스 스타일 (고정폭 글꼴)
    try:
        code_style = doc.styles.add_style('CodeStyle', WD_STYLE_TYPE.PARAGRAPH)
    except:
        code_style = doc.styles['CodeStyle']

    code_style.font.name = 'Consolas'
    code_style.element.rPr.rFonts.set(qn('w:eastAsia'), 'Malgun Gothic')
    code_style.font.size = Pt(10)
    code_style.paragraph_format.space_after = Pt(0)  # 줄간격 좁게

    # 3. 원본 텍스트 데이터 (요청하신 내용 그대로)
    raw_text = """
🚀 풀스택 개발 단계별 프롬프트 가이드
═════════════════════════════════════

📋 Phase 1: 요구사항 분석 및 기획

1.1 프로젝트 개요 정의

프로젝트의 핵심 목표와 비전을 정의해주세요:
- 프로젝트 이름: [입력]
- 해결하려는 문제: [입력]
- 타겟 사용자: [입력]
- 핵심 가치 제안: [입력]
- 프로젝트 성공 기준: [입력]

1.2 기능 요구사항 명세 (FRD)

다음 형식으로 상세 기능 목록을 작성해주세요:

[모듈명] - [기능ID]
├── 기능명: [기능 이름]
├── 설명: [상세 설명]
├── 우선순위: [Must/Should/Could/Won't]
├── 입력: [사용자 입력 데이터]
├── 출력: [시스템 출력 데이터]
├── 처리 로직: [핵심 로직 설명]
└── 예외 케이스: [에러 상황 및 처리방법]

최소 10개의 핵심 기능을 정의해주세요.

1.3 비기능 요구사항 명세 (NFR)

다음 항목별 요구사항을 정의해주세요:

성능:
- 동시 접속자 수: [예: 1000명]
- 페이지 로딩 시간: [예: 2초 이내]
- API 응답 시간: [예: 500ms 이내]

보안:
- 인증 방식: [JWT/OAuth2/Session 등]
- 데이터 암호화: [필요 여부 및 방식]
- 접근 권한 관리: [RBAC/ABAC 등]

확장성:
- 예상 사용자 수: [단기/중기/장기]
- 데이터 증가율: [예: 월 10GB]

가용성:
- uptime 목표: [예: 99.9%]
- 백업 전략: [실시간/일일/주간]

1.4 사용자 흐름 정의 (User Flow)

주요 사용자 시나리오별 플로우를 다이어그램 형태로 작성해주세요:

시나리오 1: [사용자 행동]
[시작] → [화면 A] → [액션] → [화면 B] → [결과]

각 화면에서:
- 사용자가 보는 정보
- 수행 가능한 액션
- 다음 단계로의 전환 조건
- 에러 발생 시 처리 흐름

──────────────────────────────────────────────────────────────────────────────
🛠️ Phase 2: 기술 스택 선정

2.1 아키텍처 설계

프로젝트 아키텍처를 설계해주세요:

[아키텍처 패턴 선택]
- 모노리식 / MSA / 마이크로 프론트엔드 / 기타
- 선택 이유: [근거]

[시스템 구성도]
┌─────────────────────────────────────┐
│           Client Layer              │
│    (Web/Mobile/Desktop)             │
└─────────────┬───────────────────────┘
                │
┌─────────────▼───────────────────────┐
│           API Gateway               │
│    (인증/라우팅/Rate Limiting)       │
└─────────────┬───────────────────────┘
                │
┌─────────────▼───────────────────────┐
│        Service Layer                │
│  ┌─────────┐ ┌─────────┐ ┌────────┐ │
│  │ Service │ │ Service │ │Service │ │
│  │    A    │ │    B    │ │   C    │ │
│  └─────────┘ └─────────┘ └────────┘ │
└─────────────────────────────────────┘

2.2 프론트엔드 기술 스택

프론트엔드 기술 스택을 선정하고 근거를 작성해주세요:

[핵심 프레임워크/라이브러리]
- 메인 프레임워크: [React/Vue/Angular/Svelte 등]
- 버전: [구체적 버전]
- 선택 이유: [팀 역량/생태계/성능 등]

[상태 관리]
- 전역 상태: [Redux/Zustand/Recoil/Pinia 등]
- 서버 상태: [React Query/SWR/RTK Query 등]
- 선택 이유: [데이터 복잡도/캐싱 전략 등]

[UI/스타일링]
- UI 라이브러리: [MUI/Ant Design/Chakra/Shadcn 등]
- CSS 방식: [Styled-components/Tailwind/CSS Modules 등]
- 아이콘: [Lucide/React Icons 등]

[빌드/개발 도구]
- 번들러: [Vite/Webpack/Parcel 등]
- 언어: [TypeScript/JavaScript]
- 테스트: [Jest/Vitest/Playwright/Cypress 등]
- 린팅: [ESLint/Prettier]

2.3 백엔드 기술 스택

백엔드 기술 스택을 선정하고 근거를 작성해주세요:

[런타임/언어]
- 언어: [Node.js/Python/Java/Go/Rust 등]
- 버전: [구체적 버전]
- 선택 이유: [성능/생태계/개발 속도 등]

[프레임워크]
- 웹 프레임워크: [Express/Fastify/NestJS/FastAPI/Spring 등]
- 선택 이유: [아키텍처 지원/미들웨어/문서화 등]

[데이터베이스]
- 메인 DB: [PostgreSQL/MySQL/MongoDB 등]
- 선택 이유: [데이터 구조/트랜잭션/확장성 등]
- 캐시: [Redis/Memcached]
- 검색: [Elasticsearch/Meilisearch] (필요시)

[인증/보안]
- 인증: [JWT/OAuth2/OpenID Connect]
- 비밀번호 해싱: [bcrypt/Argon2]
- HTTPS/TLS: [Let's Cloudflare/직접 관리]

[외부 서비스]
- 파일 저장: [AWS S3/Cloudflare R2/MinIO]
- 이메일: [SendGrid/AWS SES/Resend]
- 푸시 알림: [FCM/APNs/OneSignal] (필요시)

──────────────────────────────────────────────────────────────────────────────
🗄️ Phase 3: 데이터베이스 설계

3.1 ERD 설계

다음 형식으로 ERD를 작성해주세요:

[엔티티 목록]
1. users (사용자)
2. posts (게시글)
3. comments (댓글)
...

[관계 정의]
- users 1:N posts (한 사용자가 여러 게시글 작성)
- posts 1:N comments (한 게시글에 여러 댓글)
- users 1:N comments (한 사용자가 여러 댓글 작성)

[ERD 다이어그램]
┌─────────────┐       ┌─────────────┐       ┌─────────────┐
│    users    │       │    posts    │       │   comments  │
├─────────────┤       ├─────────────┤       ├─────────────┤
│ PK id       │◄──────┤ PK id       │◄──────┤ PK id       │
│    email    │       │ FK user_id  │       │ FK post_id  │
│    password │       │    title    │       │ FK user_id  │
│    name     │       │    content  │       │    content  │
│    created_at       │    created_at       │    created_at
└─────────────┘       └─────────────┘       └─────────────┘

3.2 테이블 스키마 정의

각 테이블의 상세 스키마를 정의해주세요:

Table: [테이블명]
├── [컬럼명]
│   ├── 타입: [VARCHAR(255)/INTEGER/TIMESTAMP 등]
│   ├── 제약조건: [NOT NULL/UNIQUE/DEFAULT 등]
│   ├── 인덱스: [ 필요시 ]
│   └── 설명: [컬럼 용도]
├── [컬럼명]
│   └── ...
├── Primary Key: [PK 컬럼]
├── Foreign Keys:
│   └── [컬럼] → [참조테이블].[참조컬럼] (ON DELETE CASCADE/SET NULL)
└── Indexes:
    └── [인덱스명] ON [컬럼들]

모든 테이블에 created_at, updated_at 포함

3.3 마이그레이션 전략

데이터베이스 마이그레이션 전략을 수립해주세요:

[마이그레이션 도구]
- ORM/도구: [Prisma/TypeORM/Drizzle/Alembic 등]

[마이그레이션 네이밍 규칙]
- YYYYMMDDHHMMSS_descriptive_name.sql

[마이그레이션 스크립트 예시]
-- up
CREATE TABLE users (
    id SERIAL PRIMARY KEY,
    email VARCHAR(255) UNIQUE NOT NULL,
    ...
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- down
DROP TABLE IF EXISTS users;

[시드 데이터]
- 초기 관리자 계정
- 기본 설정값
- 테스트용 더미 데이터 (개발환경)

──────────────────────────────────────────────────────────────────────────────
🔌 Phase 4: API 설계

4.1 API 명세 작성 (OpenAPI/Swagger)

OpenAPI 3.0 형식으로 API 명세를 작성해주세요:

[API 기본 정보]
- Base URL: https://api.example.com/v1
- 인증: Bearer Token (JWT)

[엔드포인트: 사용자 등록]
POST /auth/register
├── Summary: 새 사용자 회원가입
├── Tags: [Authentication]
├── Request Body:
│   ├── email: string (format: email, required)
│   ├── password: string (min: 8, max: 100, required)
│   └── name: string (min: 2, max: 50, required)
├── Responses:
│   ├── 201: User created
│   │   └── { id, email, name, created_at }
│   ├── 400: Validation error
│   │   └── { errors: [{ field, message }] }
│   └── 409: Email already exists

[엔드포인트: 로그인]
POST /auth/login
...

[엔드포인트: 게시글 목록]
GET /posts
├── Query Parameters:
│   ├── page: integer (default: 1)
│   ├── limit: integer (default: 20, max: 100)
│   ├── sort: string (enum: created_at, updated_at, views)
│   └── order: string (enum: asc, desc)
├── Responses:
│   └── 200:
│       ├── data: [Post]
│       ├── pagination:
│       │   ├── page, limit, total, totalPages
│       │   └── hasNext, hasPrev
│       └── meta: { timestamp }

4.2 에러 응답 표준화

모든 API의 에러 응답을 표준화해주세요:

[에러 응답 형식]
{
    "error": {
        "code": "ERROR_CODE",
        "message": "Human readable message",
        "details": [
            {
                "field": "fieldName",
                "message": "Specific error for this field"
            }
        ],
        "timestamp": "2024-01-01T00:00:00Z",
        "requestId": "uuid-for-debugging"
    }
}

[에러 코드 정의]
- 400: BAD_REQUEST - 잘못된 요청
- 401: UNAUTHORIZED - 인증 필요
- 403: FORBIDDEN - 권한 없음
- 404: NOT_FOUND - 리소스 없음
- 409: CONFLICT - 리소스 충돌
- 422: VALIDATION_ERROR - 유효성 검사 실패
- 429: RATE_LIMITED - 요청 초과
- 500: INTERNAL_ERROR - 서버 오류

4.3 API 버전 관리 전략

API 버전 관리 전략을 수립해주세요:

[버전 관리 방식]
- URL Path: /v1/, /v2/
- Header: Accept: application/vnd.api.v1+json
- 선택 및 근거: https://blog.naver.com/gengacademy/224163800603?fromRss=true&trackingCode=rss

[하위 호환성 정책]
- Major 버전: 하위 호환성 깨질 수 있음
- Minor 버전: 새 기능 추가, 하위 호환 유지
- Patch 버전: 버그 수정만

[Deprecated 처리]
- deprecated 필드는 6개월간 유지
- 응답에 deprecation warning 헤더 포함
- Sunset 헤더로 종료 예정일 명시

──────────────────────────────────────────────────────────────────────────────
🎨 Phase 5: 프론트엔드 설계

5.1 페이지/라우트 구조

앱의 라우트 구조를 정의해주세요:

[공개 라우트]
/               - Landing Page (LandingLayout)
├── /login      - Login Page (AuthLayout)
├── /register   - Register Page (AuthLayout)
├── /about      - About Page (MainLayout)
└── /contact    - Contact Page (MainLayout)

[인증 필요 라우트]
/dashboard            - Dashboard (MainLayout)
├── /profile          - User Profile (MainLayout)
├── /settings         - Settings (MainLayout)
│   ├── /account      - Account Settings
│   ├── /notifications - Notification Settings
│   └── /security     - Security Settings
└── /posts            - Post Management
    ├── /new          - Create Post
    └── /:id/edit     - Edit Post

[라우트 가드]
- 인증 체크: AuthGuard
- 권한 체크: RoleGuard (ADMIN, USER 등)
- 레이아웃: LayoutWrapper

5.2 컴포넌트 계층 구조

컴포넌트 아키텍처를 설계해주세요:

[src/components/]
├── atoms/              # 가장 작은 단위
│   ├── Button/
│   │   ├── Button.tsx
│   │   ├── Button.stories.tsx
│   │   └── Button.test.tsx
│   ├── Input/
│   ├── Label/
│   └── Icon/
├── molecules/          # atoms 조합
│   ├── FormField/      # Label + Input + Error
│   ├── SearchBar/      # Input + Button
│   └── CardHeader/
├── organisms/          # molecules 조합
│   ├── LoginForm/
│   ├── PostCard/
│   └── Navigation/
├── templates/          # 페이지 레이아웃
│   ├── MainLayout/
│   ├── AuthLayout/
│   └── DashboardLayout/
└── pages/              # 페이지 컴포넌트
    ├── HomePage/
    ├── LoginPage/
    └── DashboardPage/

[컴포넌트 인터페이스 예시]
interface ButtonProps {
    variant: 'primary' | 'secondary' | 'danger';
    size: 'sm' | 'md' | 'lg';
    disabled?: boolean;
    loading?: boolean;
    onClick?: () => void;
    children: React.ReactNode;
}

5.3 상태 관리 설계

상태 관리 전략을 설계해주세요:

[클라이언트 상태 (Zustand/Redux)]
store/
├── authStore.ts       # 인증 관련
│   ├── user: User | null
│   ├── isAuthenticated: boolean
│   ├── login: (credentials) => Promise<void>
│   └── logout: () => void
├── uiStore.ts         # UI 상태
│   ├── theme: 'light' | 'dark'
│   ├── sidebarOpen: boolean
│   └── toast: Toast[]
└── settingsStore.ts   # 사용자 설정

[서버 상태 (React Query)]
hooks/
├── useAuth.ts
│   ├── useLogin()
│   ├── useRegister()
│   └── useLogout()
├── usePosts.ts
│   ├── usePosts(params)       # 목록 조회
│   ├── usePost(id)            # 단일 조회
│   ├── useCreatePost()        # 생성
│   ├── useUpdatePost()        # 수정
│   └── useDeletePost()        # 삭제
└── useUsers.ts

[캐싱 전략]
- posts: staleTime: 5분, cacheTime: 30분
- user: staleTime: 10분, cacheTime: 1시간
- mutations: 낙관적 업데이트 적용

5.4 폼 처리 전략

폼 처리 아키텍처를 설계해주세요:

[라이브러리 조합]
- 유효성 검사: Zod
- 폼 상태: React Hook Form
- 에러 표시: 커스텀 FormField 컴포넌트

[스키마 정의 예시]
const loginSchema = z.object({
    email: z.string().email('올바른 이메일을 입력해주세요'),
    password: z.string().min(8, '비밀번호는 8자 이상입니다'),
    rememberMe: z.boolean().default(false)
});

type LoginFormData = z.infer<typeof loginSchema>;

[폼 컴포넌트 패턴]
function LoginForm() {
    const { register, handleSubmit, formState: { errors, isSubmitting } } =
        useForm<LoginFormData>({
            resolver: zodResolver(loginSchema)
        });

    const loginMutation = useLogin();

    const onSubmit = async (data: LoginFormData) => {
        await loginMutation.mutateAsync(data);
    };

    return (
        <form onSubmit={handleSubmit(onSubmit)}>
            <FormField
                label="이메일"
                error={errors.email?.message}
            >
                <Input {...register('email')} type="email" />
            </FormField>
            ...
        </form>
    );
}

──────────────────────────────────────────────────────────────────────────────
⚙️ Phase 6: 백엔드 개발

6.1 프로젝트 구조 설정

백엔드 프로젝트 구조를 설정해주세요:

[src/]
├── config/             # 설정 파일
│   ├── database.ts     # DB 연결
│   ├── redis.ts        # Redis 설정
│   └── env.ts          # 환경변수 검증
├── modules/            # 도메인 모듈
│   ├── auth/
│   │   ├── auth.controller.ts
│   │   ├── auth.service.ts
│   │   ├── auth.routes.ts
│   │   ├── auth.schema.ts    # Zod/Yup 검증
│   │   └── auth.middleware.ts
│   ├── users/
│   │   ├── user.entity.ts    # DB 모델
│   │   ├── user.repository.ts
│   │   ├── user.service.ts
│   │   └── user.routes.ts
│   └── posts/
│       └── ...
├── common/             # 공통 유틸리티
│   ├── middleware/
│   │   ├── errorHandler.ts
│   │   ├── rateLimiter.ts
│   │   └── validator.ts
│   ├── utils/
│   │   ├── logger.ts
│   │   ├── hash.ts
│   │   └── jwt.ts
│   └── types/
│       └── index.ts
├── database/
│   ├── migrations/
│   └── seeds/
└── app.ts              # 앱 진입점

6.2 인증/인가 시스템 구현

인증 시스템을 구현해주세요:

[JWT 전략]
- Access Token: 15분 유효, 메모리에만 저장
- Refresh Token: 7일 유효, httpOnly 쿠키
- Refresh Token Rotation: 재사용 시 모든 토큰 폐기

[구현할 기능]
1. 회원가입 (/auth/register)
   - 이메일 중복 체크
   - 비밀번호 해싱 (bcrypt, saltRounds: 12)
   - 이메일 인증 메일 발송

2. 로그인 (/auth/login)
   - 이메일/비밀번호 검증
   - JWT 발급
   - 로그인 이력 저장

3. 토큰 갱신 (/auth/refresh)
   - Refresh Token 검증
   - 새 Access/Refresh Token 발급

4. 로그아웃 (/auth/logout)
   - 토큰 블랙리스트 처리 (Redis)
   - 쿠키 삭제

[미들웨어]
- authenticate: JWT 검증
- authorize(roles): 권한 체크
- optionalAuth: 선택적 인증

6.3 CRUD API 구현

게시글 CRUD API를 구현해주세요:

[Create - POST /posts]
- 입력 검증 (제목: 1-200자, 내용: 1-10000자)
- 트랜잭션 처리
- 생성 후 캐시 무효화

[Read - GET /posts/:id]
- 존재하지 않는 ID 처리 (404)
- 조회수 증가 (비동기 처리 또는 캐시)
- 작성자 정보 포함 (JOIN)

[Read List - GET /posts]
- 페이지네이션 (cursor/offset)
- 필터링 (작성자, 날짜 범위, 키워드 검색)
- 정렬 (최신순, 인기순, 조회수순)
- 캐싱 적용 (Redis, 5분)

[Update - PATCH /posts/:id]
- 작성자 본인만 수정 가능
- optimistic locking (version 필드)
- 부분 업데이트 지원

[Delete - DELETE /posts/:id]
- Soft Delete (deleted_at 필드)
- 하위 댓글 함께 처리
- 관련 파일 삭제 (S3)

6.4 에러 처리 및 로깅

글로벌 에러 처리 시스템을 구현해주세요:

[에러 클래스 계층]
AppError (기본)
├── ValidationError (400)
├── AuthenticationError (401)
├── AuthorizationError (403)
├── NotFoundError (404)
├── ConflictError (409)
└── InternalError (500)

[에러 핸들러 미들웨어]
- 개발환경: 스택 트레이스 포함
- 프로덕션: 일반화된 메시지
- 에러 로깅 (Sentry 또는 자체 시스템)

[로깅 전략]
- Logger: Winston 또는 Pino
- 로그 레벨: error, warn, info, debug
- 로그 항목: timestamp, requestId, userId, method, url, status, duration
- 로그 저장: 파일 + ELK Stack (선택)

──────────────────────────────────────────────────────────────────────────────
💻 Phase 7: 프론트엔드 개발

7.1 프로젝트 초기화 및 설정

프론트엔드 프로젝트를 설정해주세요:

[개발 환경 설정]
1. Vite로 프로젝트 생성 (React + TypeScript)
2. ESLint + Prettier 설정
3. 절대 경로 설정 (@/components, @/hooks 등)
4. 환경변수 설정 (.env.development, .env.production)

[핵심 라이브러리 설치]
- 상태관리: npm install zustand
- 서버상태: npm install @tanstack/react-query
- 폼: npm install react-hook-form zod @hookform/resolvers
- HTTP: npm install axios
- 라우팅: npm install react-router-dom
- 스타일: npm install tailwindcss @tailwindcss/forms

[폴더 구조 생성]
mkdir -p src/{components/{atoms,molecules,organisms,templates},hooks,pages,stores,services,utils,types,styles}

7.2 API 클라이언트 설정

API 통신 레이어를 구현해주세요:

[Axios 인스턴스 설정]
- Base URL: 환경변수에서 읽기
- Timeout: 10000ms
- Retry 로직: 3회, exponential backoff

[인터셉터]
- Request: Access Token 추가
- Response 401: Refresh Token으로 갱신 시도
- Response 에러: 표준화된 에러 객체로 변환

[API 함수 구조]
services/
├── api.ts              # axios 인스턴스
├── authApi.ts
│   ├── login(data): Promise<AuthResponse>
│   ├── register(data): Promise<User>
│   └── refresh(): Promise<TokenPair>
├── postApi.ts
│   ├── getPosts(params): Promise<Paginated<Post>>
│   ├── getPost(id): Promise<Post>
│   ├── createPost(data): Promise<Post>
│   ├── updatePost(id, data): Promise<Post>
│   └── deletePost(id): Promise<void>
└── types.ts            # API 관련 타입

7.3 주요 화면 구현

핵심 페이지를 구현해주세요:

[로그인 페이지]
- 폼: 이메일, 비밀번호, 로그인 상태 유지
- 유효성 검사: 실시간 + 제출 시
- 로딩 상태: 버튼 disabled + 스피너
- 에러 표시: API 에러 메시지 표시
- 리다이렉트: 로그인 후 원래 가려던 페이지 또는 홈

[대시보드 페이지]
- 레이아웃: 사이드바 + 헤더 + 메인 콘텐츠
- 데이터: React Query로 서버 데이터 페칭
- 상태: 로딩, 에러, 빈 상태 처리
- 실시간: 필요시 WebSocket 연결

[목록 페이지]
- 테이블/카드 뷰
- 페이지네이션 또는 무한 스크롤
- 필터/검색 기능
- 정렬 기능
- 새로고침 버튼

[상세/편집 페이지]
- 읽기/편집 모드 전환
- 자동저장 (debounce 2초)
- 나가기 확인 (변경사항 있을 때)

7.4 성능 최적화

성능 최적화를 적용해주세요:

[코드 스플리팅]
- React.lazy + Suspense로 페이지 단위 코드 분할
- 중복 청크 최소화 (vendor 분리)

[데이터 Fetching 최적화]
- React Query의 prefetching (hover시)
- Infinite Query로 무한 스크롤
- Optimistic Updates로 UI 반응성 향상

[렌더링 최적화]
- React.memo로 불필요한 리렌더 방지
- useMemo/useCallback 적절히 사용
- Virtual List로 대량 데이터 렌더링 (10,000+ 아이템)

[이미지/리소스]
- WebP 포맷 사용
- Lazy loading: loading="lazy"
- Skeleton UI로 로딩 상태 표시

──────────────────────────────────────────────────────────────────────────────
🔗 Phase 8: 통합 및 테스트

8.1 단위 테스트 작성

단위 테스트를 작성해주세요:

[백엔드 테스트 (Jest/Vitest)]
- Service Layer: 비즈니스 로직 테스트
  - given: 입력 데이터
  - when: 함수 실행
  - then: 예상 결과 검증
- Repository Layer: DB 쿼리 테스트 (in-memory DB 사용)
- 유틸리티: 순수 함수 테스트

[프론트엔드 테스트 (Vitest + React Testing Library)]
- 컴포넌트 테스트
  - 렌더링 테스트
  - 사용자 상호작용 테스트 (fireEvent/userEvent)
  - props 변화 테스트
- Hook 테스트
  - custom hook 동작 검증
  - 비동기 hook 테스트
- Store 테스트
  - 상태 변경 로직 검증

[테스트 커버리지 목표]
- 핵심 비즈니스 로직: 90%+
- API 엔드포인트: 80%+
- UI 컴포넌트: 70%+

8.2 통합/E2E 테스트

통합 및 E2E 테스트를 작성해주세요:

[API 통합 테스트]
- Supertest로 HTTP 요청 테스트
- 실제 DB 사용 (test database)
- 테스트 데이터 시드/클린업

[시나리오 예시]
1. 사용자 회원가입 → 로그인 → 게시글 작성 → 조회
2. 동시 편집 시 충돌 처리
3. 인증 만료 시 자동 갱신

[E2E 테스트 (Playwright/Cypress)]
- 주요 사용자 시나리오
  - 회원가입 및 로그인 플로우
  - 게시글 작성 및 관리
  - 설정 변경
- 크로스 브라우저 테스트 (Chrome, Firefox, Safari)
- 모바일 반응형 테스트
- 테스트 데이터 격리 (각 테스트마다 새 사용자)

8.3 보안 테스트

보안 테스트를 수행해주세요:

[OWASP Top 10 점검]
- SQL Injection: Prepared Statement 확인
- XSS: 입력 sanitization, CSP 헤더
- CSRF: Token 기반 보호
- 인증: Brute force 방지 (rate limiting)
- 노출: .env, API 키 노출 여부

[의존성 취약점 검사]
- npm audit / pip-audit
- Dependabot 설정
- 주기적 업데이트 스케줄

[침투 테스트]
- 인증 우회 시도
- 권한 상승 시도
- 민감 데이터 접근 시도

──────────────────────────────────────────────────────────────────────────────
🚀 Phase 9: 배포 및 운영

9.1 CI/CD 파이프라인

CI/CD 파이프라인을 구축해주세요:

[GitHub Actions Workflow]
1. CI Pipeline (Pull Request)
   - 코드 체크아웃
   - 의존성 설치
   - Lint 검사
   - 타입 체크
   - 단위 테스트 실행
   - 테스트 커버리지 리포트

2. CD Pipeline (Merge to main)
   - CI 단계 모두 통과
   - Docker 이미지 빌드
   - 보안 스캔 (Trivy/Snyk)
   - Staging 환경 배포
   - E2E 테스트 (Staging)
   - Production 배포 (승인 후)

[배포 전략]
- Blue-Green 배포 (무중단)
- 또는 Rolling Update
- 롤백 전략: 이전 버전 자동 롤백 (헬스체크 실패시)

9.2 인프라 구성

인프라를 구성해주세요:

[Docker 설정]
# Dockerfile (멀티스테이지)
- Stage 1: Build (Node.js + 의존성 설치 + 빌드)
- Stage 2: Production (nginx + 정적 파일)

# docker-compose.yml (개발환경)
- app: Node.js 앱
- db: PostgreSQL
- redis: Redis 캐시
- nginx: 리버스 프록시

[클라우드 배포 (AWS 예시)]
- Frontend: CloudFront + S3
- Backend: ECS Fargate 또는 EC2
- Database: RDS PostgreSQL
- Cache: ElastiCache Redis
- 파일 저장: S3
- DNS: Route 53
- SSL: ACM (AWS Certificate Manager)

[IaC (Terraform/Pulumi)]
- 인프라 코드화
- 환경별 구성 분리 (dev/staging/prod)
- 상태 관리: S3 + DynamoDB

9.3 모니터링 및 알림

모니터링 시스템을 구축해주세요:

[애플리케이션 모니터링]
- APM: Datadog 또는 New Relic
- 또는 OpenTelemetry + Jaeger (분산 추적)
- 에러 추적: Sentry
- 로그 집계: ELK Stack 또는 CloudWatch

[메트릭 수집]
- CPU, Memory 사용량
- API 응답 시간 (P50, P95, P99)
- 에러율 (4xx, 5xx 비율)
- 비즈니스 메트릭 (DAU, 주요 기능 사용량)

[알림 설정]
- PagerDuty 또는 Slack 연동
- 심각도 구분:
  - Critical: 즉시 호출 (서버 다운)
  - Warning: 슬랙 알림 (에러율 상승)
  - Info: 대시보드 기록

[헬스체크]
- /health: 기본 서버 상태
- /health/db: 데이터베이스 연결
- /health/redis: 캐시 연결
- Kubernetes readiness/liveness probe

──────────────────────────────────────────────────────────────────────────────
📚 Phase 10: 문서화 및 인수인계

10.1 기술 문서 작성

기술 문서를 작성해주세요:

[README.md]
- 프로젝트 개요
- 기술 스택
- 로컬 개발 환경 설정
- 테스트 실행 방법
- 배포 방법

[API 문서]
- Swagger UI 또는 Postman Collection
- 각 엔드포인트별 예시 요청/응답
- 인증 방법 설명
- 에러 코드 사전

[아키텍처 문서]
- 시스템 구성도
- 데이터 흐름도
- 배포 아키텍처
- 보안 구조

[개발 가이드]
- 코드 컨벤션
- Git 브랜치 전략 (Git Flow 또는 Trunk-based)
- PR 템플릿
- 코드 리뷰 가이드라인

──────────────────────────────────────────────────────────────────────────────
✅ 각 단계별 체크리스트

각 단계를 시작할 때 다음 체크리스트를 확인하세요:

[ ] Phase 1: 요구사항 확정 및 문서화 완료
[ ] Phase 2: 기술 스택 선정 및 개발환경 구축
[ ] Phase 3: DB 스키마 확정 및 마이그레이션 준비
[ ] Phase 4: API 명세 확정 및 Mock API 준비
[ ] Phase 5: UI/UX 설계 완료 및 컴포넌트 정의
[ ] Phase 6: 백엔드 개발 완료 및 단위 테스트 통과
[ ] Phase 7: 프론트엔드 개발 완료 및 단위 테스트 통과
[ ] Phase 8: 통합 테스트 및 E2E 테스트 통과
[ ] Phase 9: 스테이징 배포 및 성능 테스트 완료
[ ] Phase 10: 프로덕션 배포 및 모니터링 설정
"""

    lines = raw_text.split('\n')

    import re

    # 4. 내용 파싱 및 문서 작성
    for line in lines:
        line = line.strip()  # 앞뒤 공백 제거

        # 헤더 1 (Phase X) 감지
        if re.match(r'^.*Phase \d+:', line) or re.match(r'^🚀.*Phase \d+', line) or re.match(r'^🛠️.*Phase \d+',
                                                                                            line) or re.match(
                r'^🗄️.*Phase \d+', line) or re.match(r'^🔌.*Phase \d+', line) or re.match(r'^🎨.*Phase \d+',
                                                                                         line) or re.match(
                r'^⚙️.*Phase \d+', line) or re.match(r'^💻.*Phase \d+', line) or re.match(r'^🔗.*Phase \d+',
                                                                                         line) or re.match(
                r'^📚.*Phase \d+', line):
            doc.add_heading(line, level=1)
        # 헤더 2 (1.1, 2.3 등) 감지
        elif re.match(r'^\d+\.\d+\s', line):
            doc.add_heading(line, level=2)
        # 구분선 (─, ═) 무시 (문서 깔끔하게 유지)
        elif set(line).issubset({'─', '═', ' ', '│', '┌', '└', '├', '┤', '┴', '┬', '┼'}) and len(
                line) > 5 and '│' not in line:
            pass  # 긴 구분선은 생략하거나 필요한 경우 hr 추가
        # 체크리스트 헤더
        elif "✅" in line:
            doc.add_heading(line, level=1)
        # 빈 줄은 추가
        elif not line:
            doc.add_paragraph("")
        # 그 외 모든 내용은 'CodeStyle'로 추가하여 정렬 유지
        else:
            # 다이어그램이나 구조화된 텍스트가 많으므로 일반 텍스트도 고정폭으로 처리
            # 가독성을 위해 들여쓰기나 특수문자가 포함된 경우 CodeStyle 적용
            # 일반 설명글과 프롬프트 템플릿을 구분하기 어려우므로,
            # 본문의 정렬(트리 구조 등)을 살리기 위해 전체를 CodeStyle로 하되
            # 폰트를 맑은 고딕(한글) + Consolas(영문)으로 처리했으므로 자연스러움
            p = doc.add_paragraph(line, style='CodeStyle')

            # 폰트 강제 적용 (안전장치)
            for run in p.runs:
                run.font.name = 'Consolas'
                run.element.rPr.rFonts.set(qn('w:eastAsia'), 'Malgun Gothic')

    # 5. 저장
    file_path = 'full_stack_prompt_guide_v2.docx'
    doc.save(file_path)
    return file_path


create_full_stack_prompt_guide()